---
- name: Set {{ sys_xterm }} as terminal emulator 
  when: sys_xterm is defined
  community.general.alternatives:
    name: x-terminal-emulator
    path: "{{ sys_xterm }}"
    link: /usr/bin/x-terminal-emulator
    priority: 90
